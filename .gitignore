node_modules/
package-lock.json
# Git worktrees the agent harness checks out inside the repo — transient scratch
# copies of the tree, never part of a commit. See jest.config.js's matching ignore.
.claude/worktrees/
# Generated native projects (managed Expo workflow — prebuild output, never committed)
/ios/
/android/
.expo/
.DS_Store
.env
.env.local
*.log
*.swp
*.swo
*~
.vscode/
.idea/
dist/
build/
preview-shots*/
# Generated design-review package (npm run review-bundle) — screenshots + collected
# sources + the connection map. Regenerated on demand; never committed (14MB of PNGs
# per run would sit in git history forever).
review-bundle/
review-bundle.zip
coverage/
# Signing / submit secrets — never commit
*.p8
asc-api-key.p8
google-play-service-account.json
